# ===========================================
# CCIS PMS — Client Environment Variables
# ===========================================
# Copy this file to .env and fill in the values.
# Variables must be prefixed with VITE_ to be available in the frontend.

# API Base URL (optional — defaults to '/api' which is proxied by Vite dev server)
VITE_API_URL=/api
