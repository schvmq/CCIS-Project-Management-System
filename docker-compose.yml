version: "3.8"

services:
  db:
    image: postgres:16-alpine
    container_name: ccis_pms_db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-ccis_admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-ccis_dev_password}
      POSTGRES_DB: ${POSTGRES_DB:-ccis_pms}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ccis_admin}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
